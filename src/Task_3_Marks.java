public class Task_3_Marks {
    public static void main(String[] args) {
        System.out.println("!" + "!"); // just sums up the strings, so it becomes "!!"
        // System.out.println((char)("!"+"!"));
        /* the code does not compile because of an error.
        It occurs because of the wrong type of the symbols inside char,
        which only takes an integer but not a string
         */
    }
}
